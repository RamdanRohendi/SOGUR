<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.solusinganggur">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SolusiNganggur">
        <activity
            android:name=".PerusahaanDaftarPelamarActivity"
            android:label="@string/title_activity_perusahaan_daftar_pelamar"
            android:theme="@style/Theme.SolusiNganggur.NoActionBar"></activity>
        <activity android:name=".PerusahaanMenuActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" /> <!-- Auth Layout -->
        <activity android:name=".AuthSplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AuthIntroSatuActivity" />
        <activity android:name=".AuthIntroDuaActivity" />
        <activity android:name=".AuthPilihRoleActivity" />
        <activity android:name=".PencariKerjaDaftarActivity" />
        <activity android:name=".PerusahaanDaftarActivity" />
        <activity android:name=".AuthLoginActivity" />
        <activity android:name=".AuthLupaPasswordActivity" />
        <activity android:name=".AuthKonfirmasiUbahPasswordActivity" />
        <activity android:name=".AuthUbahPasswordActivity" />
        <activity android:name=".AuthNotifPasswordDiubahActivity" />
        <activity android:name=".AuthAfterLoginActivity" /> <!-- Layout Role Pelamar -->
        <activity android:name=".PencariKerjaMenuActivity" />
        <activity android:name=".PencariKerjaNotifikasiActivity" />
        <activity android:name=".PencariKerjaAcceptedActivity" />
        <activity android:name=".PencariKerjaPendingActivity" />
        <activity android:name=".PencariKerjaRejectedActivity" />
        <activity android:name=".PencariKerjaDetailAcceptedActivity" />
        <activity android:name=".PencariKerjaDetailPendingActivity" />
        <activity android:name=".PencariKerjaDetailRejectedActivity" />
        <activity android:name=".PencariKerjaEditProfileActivity" /> <!-- Layout Role Perusahaan -->
        <activity android:name=".PerusahaanAddLowonganActivity" /> <!-- Layout All User -->
        <activity android:name=".UserMessageActivity" />
        <activity android:name=".UserMessage6Activity" />
        <activity android:name=".UserBantuanActivity" />
        <activity android:name=".UserInfoApkActivity" />
        <activity android:name=".UserKontakKamiActivity" />
    </application>

</manifest>